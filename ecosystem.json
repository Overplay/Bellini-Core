{
  /**
   * Application configuration section
   * http://pm2.keymetrics.io/docs/usage/application-declaration/
   */
  apps : [

    // First application
    {
      name      : "Asahi",
      script    : "app.js",
      env: {
        COMMON_VARIABLE: "true"
      },
      env_production : {
        NODE_ENV: "production"
      }
    }

  ],

  /**
   * Deployment section
   * http://pm2.keymetrics.io/docs/usage/deployment/
   */
  deploy : {
    production : {
      user : "cgrigsby",
      host : "104.131.145.36",
      ref  : "origin/master",
      repo : "git@github.com:Overplay/asahi",
      path : "/opt/prod",
      "post-deploy" : "npm install && cd assets && bower update && cd .. && cp /home/asahi/local.js config/ && pm2 startOrRestart ecosystem.json --env production"
    },
    dev : {
      user : "cgrigsby",
      host : "104.131.145.36",
      ref  : "origin/develop-cole",
      repo : "git@github.com:Overplay/asahi",
      path : "/opt/dev",
      "post-deploy" : "npm install && cd assets && bower update && cd .. && cp /home/asahi/local.js config/ && pm2 startOrRestart ecosystem.json --env dev",
      env  : {
        NODE_ENV: "dev"
      }
    }
  }  
}
